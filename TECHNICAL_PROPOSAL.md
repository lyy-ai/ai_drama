# AI 短剧平台 技术方案（评审稿）

- 日期：2026-07-18
- 服务器：2 × A100-40GB（GPU0 被其他任务占用约 25G，GPU1 空闲约 34G）
- 对外地址：`http://36.212.51.4`，开放端口 **10045–10050**
- 已有基础：Wan2.1 已部署（`/data/liyangyang/Wan2.1`），T2V-1.3B 权重已就位，`qwen35_env` 可复用

---

## 1. 总体架构

```
用户浏览器
   │  http://36.212.51.4:10045
   ▼
┌─────────────────────────────────────────────────────────┐
│ 前端 Web UI (Vue3 + Vite + Element Plus)      :10045    │
└──────────────────────┬──────────────────────────────────┘
                       │ REST / WebSocket
                       ▼
┌─────────────────────────────────────────────────────────┐
│ 后端编排服务 FastAPI（流水线引擎 + 任务队列） :10046    │
│  - 项目管理 / 剧本确认 / 分镜编辑 / 一键生成            │
│  - SQLite 存项目与任务状态；WebSocket 推送进度          │
│  - GPU 资源调度（串行调度生成类任务，防显存争抢）       │
└───┬─────────┬─────────┬─────────┬───────────────────────┘
    │HTTP     │HTTP     │HTTP     │HTTP
    ▼         ▼         ▼         ▼
 LLM服务   ComfyUI   Wan2.1    CosyVoice2
 剧本生成  图像生成   视频生成   语音合成
 :10048    :10047    :10050    :10049
 Ollama   SDXL/Flux  I2V/T2V   多音色TTS
 Qwen3    角色一致性  已部署✅   情绪控制
```

端口分配（全部绑定 `0.0.0.0`，防火墙放行 10045-10050）：

| 端口 | 服务 | 说明 |
|---|---|---|
| 10045 | 前端静态站（Nginx 或 vite preview） | 用户入口 |
| 10046 | 后端编排 API（FastAPI + Uvicorn） | 前端只和它通信 |
| 10047 | ComfyUI API | 图像生成工作流 |
| 10048 | Ollama（Qwen3） | 剧本/分镜/台词生成 |
| 10049 | CosyVoice2 服务（FastAPI 封装） | 配音 |
| 10050 | Wan2.1 视频生成服务（FastAPI 封装） | 视频片段生成 |

> 安全建议：仅 10045/10046 对公网开放，10047–10050 绑定 127.0.0.1 仅本机内部调用（ComfyUI/Ollama 无鉴权，直接暴露有风险）。下文端口表不变，bind 地址分开配置。

---

## 2. 流水线设计（一键式）

用户在首页粘贴「小说梗概 + 风格选择 + 集数」→ 点【生成短剧】→ 后端按 DAG 依次执行：

```
S0 剧本生成(LLM)        → 人工可编辑确认（可跳过=全自动）
S1 角色原画(ComfyUI)    → 每个角色 1 张定妆照，人工可重抽
S2 分镜关键帧(ComfyUI)  → 每个镜头 1 张首帧图（融入角色参考）
S3 视频片段(Wan2.1 I2V) → 每镜头 3-5s 片段
S4 台词配音(CosyVoice2) → 每句台词 wav，按角色分配音色
S5 合成(FFmpeg)         → 音画对齐、镜头拼接、烧字幕、BGM → 成片 mp4
```

### S0 剧本生成（Ollama + Qwen3，:10048）
- Prompt 模板输出**结构化 JSON**（约束格式，便于程序消费）：
```json
{
  "title": "...", "style": "古风/都市/悬疑",
  "characters": [{"name":"...","appearance":"发型服装外貌英文prompt","voice":"音色id","personality":"..."}],
  "episodes": [{"index":1,"shots":[{"shot_id":1,"scene":"...","camera":"特写/中景/远景",
      "first_frame_prompt":"英文绘图prompt","dialogue":[{"character":"...","line":"...","emotion":"..."}],
      "duration":4,"action":"动作描述"}]}]
}
```
- 用 `format: json` + 温度 0.7，失败自动重试 2 次；生成后进入「剧本确认页」，用户可改台词/分镜再进入后续阶段（也支持跳过确认全自动）。

### S1 角色原画（ComfyUI，:10047）
- 底模 SDXL（或 Flux.1-dev，显存够时），预置工作流 JSON，后端用 `/prompt` API 提交。
- **一致性手段**：角色名+固定 seed + 统一外貌描述词；进阶挂 IP-Adapter/LoRA（二期）。
- 每个角色出 4 张候选，前端让用户选 1 张作为「定妆照」，存入角色库复用。

### S2 分镜关键帧（ComfyUI，:10047）
- 镜头首帧 = 场景描述 + 角色外貌词 + 镜头景别；用 S1 定妆照做 img2img/IP-Adapter 参考，保证人物不崩。
- 分辨率统一 832×480（与 Wan2.1-480P 对齐，免缩放）。

### S3 视频片段生成（Wan2.1，:10050）
- **关键决策点（需您拍板，见 §6-Q1）**：
  - 方案A（推荐一期）：`T2V-1.3B` 已就位，prompt 中嵌入角色外貌描述保一致性，5 分钟/片段，最快上线。
  - 方案B（推荐二期）：下载 `Wan2.1-I2V-14B-480P`（约 30G），S2 首帧图直接做图生视频，一致性最好，但单片段约 10-20 分钟。
- 服务封装：FastAPI + 单任务 GPU 队列，内部调 `generate.py`，完成后回调编排服务。

### S4 配音（CosyVoice2，:10049）
- 预置 4-6 个音色（男/女/老/少），角色绑定音色 id。
- 支持情绪标签（`[laughter]`、`[breath]` 等）与语速调节；每句台词生成 wav + 实际时长，供 S5 对齐。

### S5 合成（FFmpeg，编排服务本地执行）
- 对齐策略：若配音时长 > 视频片段，视频尾部 `tpad` 静帧延长；否则音频 `apad` 补静音。
- 字幕：由台词 JSON 生成 ASS/SRT，按音频起止时间烧入。
- 片头片尾、转场（xfade 淡入淡出）、BGM 混音（音量 ducking）。
- 输出 `output/{project_id}/final.mp4`，前端在线播放/下载。

---

## 3. 目录与数据

```
/data/liyangyang/ai_drama/
├── TECHNICAL_PROPOSAL.md   ← 本文档
├── backend/                ← 编排服务 :10046
│   ├── app/ (api/, pipeline/, services/, models/)
│   ├── prompts/            ← 剧本 prompt 模板
│   └── data/drama.db       ← SQLite
├── frontend/               ← Vue3 :10045
├── services/
│   ├── comfyui/            ← ComfyUI 安装目录 :10047
│   ├── tts/                ← CosyVoice2 封装 :10049
│   └── video/              ← Wan2.1 封装 :10050（调用现有 /data/liyangyang/Wan2.1）
├── workflows/              ← ComfyUI 工作流 JSON 模板
└── output/{project_id}/    ← 剧本.json/角色图/分镜图/clips/音频/final.mp4
```

环境策略（不污染外部）：
- 新建 `drama_backend` venv：FastAPI/SQLAlchemy/httpx 等轻量依赖。
- 视频服务复用 `qwen35_env`（已含 Wan2.1 全部依赖）。
- ComfyUI、CosyVoice2 各自独立 venv（依赖重且易冲突，隔离最安全）。

## 4. GPU 调度策略（40G 显存要省着用）

| 服务 | 显存 | 常驻？ |
|---|---|---|
| Ollama Qwen3-8B(GPTQ/gguf) | ~6-8G | GPU1 常驻（响应快） |
| ComfyUI SDXL | ~8-10G | 按需加载，跑完 `--cpu-vae` 卸载 |
| Wan2.1 T2V-1.3B | ~24G 峰值 | 按需加载，串行执行 |
| CosyVoice2 | ~4-6G | 按需加载 |

- 编排服务内置**全局 GPU 任务锁**：S2/S3/S4 同一时刻只跑一个生成任务，其余排队；LLM 小模型常驻不冲突。
- 二期上 I2V-14B 时启用 `--offload_model True --t5_cpu`，单卡可跑。

## 5. 前端页面（:10045）

1. **首页/项目列表**：新建项目（梗概+风格+集数+时长）、进度卡片。
2. **剧本工作台**：分集分镜树形编辑、台词改写、角色卡（音色/定妆照选择）。
3. **生成监控页**：流水线 DAG 实时进度（WebSocket），每个镜头状态（图✓→视频✓→配音✓），单镜头可【重生成】。
4. **成片页**：在线播放器、逐镜头预览对比、下载 mp4/素材包。

## 6. 决策记录（2026-07-18 已确认）

- **Q1 视频方案**：一期用 T2V-1.3B（已就位，5 分钟/片段），二期可升级 I2V-14B 提升一致性。
- **Q2 LLM**：vLLM 直接加载现有 `/data/liyangyang/models/Qwen3.5-9B`，服务端口 10048，不装 Ollama。
- **Q3 绘图底模**：SDXL（约 8G 显存，秒级出图）。
- **Q4 画面比例**：竖屏 **480×832**（抖音风），Wan2.1 原生支持，全链路统一该尺寸。
- **Q5 语言**：界面、剧本、配音全中文。

## 7. 里程碑（预估）

| 阶段 | 内容 | 预计 |
|---|---|---|
| M1 | 目录/环境初始化；ComfyUI+SDXL、CosyVoice2、LLM 服务全部装好并冒烟通过 | 0.5-1 天（主要耗时在下载 ~40G 模型） |
| M2 | 后端编排 + 五个服务打通，命令行可一键跑通 S0→S5 | 1 天 |
| M3 | 前端三页 + WebSocket 进度 + 单镜头重生成 | 1 天 |
| M4 | 联调优化（字幕对齐、转场、GPU 调度）、压测、写 README | 0.5 天 |

## 8. 风险与对策

1. **人物一致性不足** → 一期 prompt 约束 + 角色定妆照；二期 IP-Adapter / I2V-14B。
2. **显存争抢崩溃** → 全局 GPU 锁 + 按需加载；所有服务加显存超限自动重启。
3. **模型下载慢** → 全部走 ModelScope 镜像（Wan2.1 已验证 10MB/s）。
4. **LLM 输出 JSON 不稳** → `format:json` 强约束 + 校验重试 + 前端人工兜底编辑。
5. **长项目排队久** → 任务粒度到「单镜头」，支持断点续跑与部分重生成。
